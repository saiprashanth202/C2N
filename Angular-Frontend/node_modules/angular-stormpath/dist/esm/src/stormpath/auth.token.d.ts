/**
 * Class to represent a Stormpath Authentication Token.
 */
export declare class AuthToken {
    accessToken: string;
    refreshToken: string;
    tokenType: string;
    expiresIn: number;
    expiresAt: number;
    exp: Date;
    static isValid(token: AuthToken): boolean;
    constructor(accessToken: string, refreshToken: string, tokenType: string, expiresIn: number, expiresAt: number, exp: Date);
}
